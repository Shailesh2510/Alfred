import React from 'react';
import StyledRadioContainer from "./styled-radio.style";

const StyledRadio: any = (props: any) => {
  return <StyledRadioContainer {...props} />;
};

export default StyledRadio;
