import React from "react";
import StyledCheckboxContainer from "./styled-checkbox.style";

const StyledCheckbox: any = (props: any) => {
  return <StyledCheckboxContainer {...props} />;
};

export default StyledCheckbox;
