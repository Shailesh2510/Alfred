import React from "react";
import StyledDateTimePickerContainer from "./styled-date-time-picker.style";

const StyledDatePicker: any = (props: any) => {
  return <StyledDateTimePickerContainer {...props} />;
};

export default StyledDatePicker;
