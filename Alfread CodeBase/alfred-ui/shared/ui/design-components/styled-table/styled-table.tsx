import React from "react";
import StyledTableComponent from "./styled-table.style";

const StyledTable: any = (props: any) => {
  return <StyledTableComponent {...props} />;
};

export default StyledTable;
