import styled from "@emotion/styled";
import { Radio } from "@mantine/core";

const StyledRadioGroupContainer: any = styled(Radio.Group)`
  .mantine-RadioGroup-label {
    margin-bottom: 8px;
  }
`;

export default StyledRadioGroupContainer;
