import React from "react";
import StyledDatePickerContainer from "./styled-date-picker.style";

const StyledDatePicker: any = (props: any) => {
  return <StyledDatePickerContainer {...props} />;
};

export default StyledDatePicker;
