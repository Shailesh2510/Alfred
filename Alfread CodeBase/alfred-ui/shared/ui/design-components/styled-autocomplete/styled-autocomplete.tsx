import React from 'react';
import { Autocomplete } from '@mantine/core';

const StyledAutoComplete: any = (props: any) => (
  <Autocomplete
    {...props}
    // Add any additional props or styles here
  />
);

export default StyledAutoComplete;