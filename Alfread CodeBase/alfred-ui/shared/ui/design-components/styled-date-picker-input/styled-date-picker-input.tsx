import React from "react";
import StyledDatePickerInputContainer from "./styled-date-picker-input.style";

const StyledDatePickerInput: any = (props: any) => {
  return <StyledDatePickerInputContainer {...props} />;
};

export default StyledDatePickerInput;
