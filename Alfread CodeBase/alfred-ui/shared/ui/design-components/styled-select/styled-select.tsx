import React from "react";
import StyledSelectComponent from "./styled-select.style";

const StyledSelect: any = (props: any) => {
  return <StyledSelectComponent {...props} />;
};

export default StyledSelect;
