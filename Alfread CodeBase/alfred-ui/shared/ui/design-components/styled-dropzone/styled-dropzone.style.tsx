import styled from "@emotion/styled";
import { Dropzone } from "@mantine/dropzone";

export const StyledDropzoneComponent: any = styled(Dropzone)`
  .mantine-Dropzone-root {
    padding: 0 !important;
  }
  .mantine-Dropzone-inner {
    height: 100%;
    padding: 0 !important;
  }
`;
