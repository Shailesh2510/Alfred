import styled from "@emotion/styled";
import { colors, typography } from "@/design-system";

export const NoDataLabel: any = styled.div`
  margin-top: 20px;
  color: ${colors.dark[9]};
  ${typography.lg400};
`;
