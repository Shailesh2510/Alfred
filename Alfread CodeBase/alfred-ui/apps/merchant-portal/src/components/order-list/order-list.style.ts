import styled from "@emotion/styled"

export const TotalPrice: any = styled.div`
	color: ${({ theme }) => theme.colors.dark[5]};
	${({ theme }) => theme.other.typography.md700};
`

export const StyledTableRow: any = styled.tr`
	cursor: pointer;
`
