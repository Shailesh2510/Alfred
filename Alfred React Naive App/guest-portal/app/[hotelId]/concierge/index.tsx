import ConciergeServiceContainer from '@/src/screens/concierge/ConciergeServiceContainer'
import ScrollableContainer from '@components/ui/ScrollableContainer'

const ConciergeServiceScreen = (): JSX.Element => {
	return (
		<ScrollableContainer>
			<ConciergeServiceContainer />
		</ScrollableContainer>
	)
}

export default ConciergeServiceScreen
