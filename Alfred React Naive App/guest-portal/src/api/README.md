## API Layer

This directory contains all API-related code:

- `api/`: API client configurations and instances
- `endpoints/`: API endpoint definitions and request/response types
- `mutations/`: React Query mutation hooks
- `queries/`: React Query query hooks

Best Practices:
- Keep API logic separate from UI components
- Use React Query for data fetching and caching
- Define strong TypeScript types for all API responses